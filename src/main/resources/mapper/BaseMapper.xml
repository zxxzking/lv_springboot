<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxxz.lv.mapper.BaseMapper">

    <update id="doAddOnlineUser">
        UPDATE online_count SET count = (count+1) WHERE id = 1
    </update>

    <update id="doDestroyOnlineUser">
        UPDATE online_count SET count = (count-1) WHERE id = 1
    </update>

    <update id="doClean">
        UPDATE online_count SET count = 0 WHERE id = 1
    </update>

</mapper>